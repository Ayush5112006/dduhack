# Email OTP System - Visual Architecture & Flow Guide

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT SIDE (React)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐         ┌──────────────────────┐           │
│  │  Registration    │         │  OTP Verification    │           │
│  │  Page            │         │  Page                │           │
│  │  /auth/register  │         │  /verify-otp         │           │
│  │                  │         │                      │           │
│  │ • Name input     │         │ • 6-digit input      │           │
│  │ • Email input    │────────▶│ • Countdown timer    │           │
│  │ • Password input │         │ • Attempt counter    │           │
│  │ • Strength meter │         │ • Resend button      │           │
│  │ • Submit button  │         │ • Success animation  │           │
│  └──────────────────┘         └──────────────────────┘           │
│           │                              │                        │
│           │ POST /api/auth/register      │ POST /api/auth/       │
│           │ {name, email, password}      │ verify-otp            │
│           │                              │ {email, otp}          │
│           │ POST /api/auth/resend-otp    │                       │
│           │ {email}                      │                       │
│           ▼                              ▼                        │
└─────────────────────────────────────────────────────────────────┘
                                │
                                │ HTTP/JSON
                                │
┌─────────────────────────────────────────────────────────────────┐
│                   SERVER SIDE (Next.js APIs)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         POST /api/auth/register                          │   │
│  │  ────────────────────────────────────────────────────    │   │
│  │  1. Validate inputs (email, password format)            │   │
│  │  2. Check if user already exists                        │   │
│  │  3. Hash password with bcryptjs (10 rounds)             │   │
│  │  4. Create user in database (emailVerified = false)     │   │
│  │  5. Generate 6-digit OTP                                │   │
│  │  6. Save OTP to EmailOTP table (10 min expiry)         │   │
│  │  7. Send OTP email (async, non-blocking)               │   │
│  │  8. Return 201 with success message                     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         POST /api/auth/verify-otp                        │   │
│  │  ────────────────────────────────────────────────────    │   │
│  │  1. Validate OTP format (6 digits)                      │   │
│  │  2. Fetch EmailOTP from database by email               │   │
│  │  3. Check if OTP expired (10 minutes)                   │   │
│  │  4. Check attempts < 5                                  │   │
│  │  5. If OTP matches:                                     │   │
│  │     - Set user.emailVerified = true                     │   │
│  │     - Delete OTP record                                 │   │
│  │     - Send welcome email (async)                        │   │
│  │  6. If OTP wrong:                                       │   │
│  │     - Increment attempts counter                        │   │
│  │     - Return 401 with remaining attempts                │   │
│  │  7. Return 200 or error response                        │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         POST /api/auth/resend-otp                        │   │
│  │  ────────────────────────────────────────────────────    │   │
│  │  1. Check if already verified (prevent bypass)          │   │
│  │  2. Check rate limit (max 1 per 5 minutes)             │   │
│  │  3. Delete old OTP                                      │   │
│  │  4. Generate new 6-digit OTP                            │   │
│  │  5. Save with new 10-minute expiry                      │   │
│  │  6. Send OTP email (async)                              │   │
│  │  7. Return 200 (never reveal if email exists)           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                    │
│                              ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │    UTILITIES & SERVICES                                  │   │
│  │  ──────────────────────────────────────────────────      │   │
│  │                                                          │   │
│  │  lib/otp-generator.ts:                                  │   │
│  │  • generateOTP() → "123456"                            │   │
│  │  • getOTPExpirationTime() → Date + 10 min              │   │
│  │  • isOTPExpired(date) → boolean                        │   │
│  │  • formatOTPForDisplay(otp) → "123 456"               │   │
│  │                                                          │   │
│  │  lib/email-service.ts:                                  │   │
│  │  • sendOTPEmail(email, otp)                            │   │
│  │  • sendWelcomeEmail(email, name)                       │   │
│  │  • Nodemailer SMTP config (Gmail)                      │   │
│  │  • HTML email templates                                │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                    │
│                              ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │    DATABASE (PostgreSQL + Prisma)                        │   │
│  │  ──────────────────────────────────────────────────      │   │
│  │                                                          │   │
│  │  User Table:                                            │   │
│  │  id | name | email | password | emailVerified           │   │
│  │                                                          │   │
│  │  EmailOTP Table:                                        │   │
│  │  id | email | otp | expiresAt | attempts | createdAt   │   │
│  │                                                          │   │
│  │  Index: [email, expiresAt] for performance             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                    │
│                              ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │    EMAIL SERVICE (Nodemailer + Gmail SMTP)              │   │
│  │  ──────────────────────────────────────────────────      │   │
│  │                                                          │   │
│  │  SMTP: smtp.gmail.com:587                              │   │
│  │  Auth: EMAIL_USER + EMAIL_PASS (App Password)          │   │
│  │  TLS: Enabled                                          │   │
│  │                                                          │   │
│  │  OTP Email:                                             │   │
│  │  • Subject: "Your OTP Code"                            │   │
│  │  • HTML body with styled OTP display                   │   │
│  │  • Shows expiry time                                    │   │
│  │                                                          │   │
│  │  Welcome Email:                                         │   │
│  │  • Subject: "Welcome to HackHub!"                      │   │
│  │  • Personalized with user name                         │   │
│  │  • Action button (optional)                            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 User Registration Flow

```
START
  │
  ▼
┌──────────────────────────────────┐
│ User visits /auth/register       │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ User enters:                     │
│ • Full Name                      │
│ • Email                          │
│ • Password                       │
│ • Confirm Password               │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Frontend Validation:             │
│ ✓ All fields filled?             │
│ ✓ Email format valid?            │
│ ✓ Password length >= 8?          │
│ ✓ Passwords match?               │
│ ✓ Password strength >= 3?        │
└──────────────────────────────────┘
  │ Valid
  ▼
┌──────────────────────────────────┐
│ POST /api/auth/register          │
│ {name, email, password}          │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Server Validation:               │
│ ✓ Email format valid?            │
│ ✓ Password length >= 8?          │
│ ✓ Name length >= 2?              │
│ ✓ Email not already used?        │
└──────────────────────────────────┘
  │ Valid
  ▼
┌──────────────────────────────────┐
│ Password Hashing:                │
│ bcryptjs.hash(password, 10)      │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Create User:                     │
│ INSERT INTO User (                │
│   name, email, hashedPassword,   │
│   emailVerified = false          │
│ )                                │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Generate OTP:                    │
│ otp = generateOTP() // "123456"  │
│ expires = now + 10 minutes       │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Save OTP:                        │
│ INSERT INTO EmailOTP (            │
│   email, otp, expiresAt          │
│ )                                │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Send OTP Email (async):          │
│ To: user@example.com             │
│ Subject: "Your OTP Code"         │
│ Body: HTML with OTP display      │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Return 201 Success:              │
│ {                                │
│   success: true,                 │
│   message: "OTP sent",           │
│   email: "user@example.com"      │
│ }                                │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Redirect to:                     │
│ /verify-otp?email=user@examp...  │
└──────────────────────────────────┘
  │
  ▼
END (User now on OTP page)
```

---

## 🔐 OTP Verification Flow

```
START (User on /verify-otp?email=...)
  │
  ▼
┌──────────────────────────────────┐
│ User receives email with OTP     │
│ Example: "Your code is 123 456"  │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ User enters 6-digit OTP:         │
│ [ 1 ][ 2 ][ 3 ][ 4 ][ 5 ][ 6 ]  │
│ (Auto-focus on each digit)       │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Timer Shows: ⏰ 09:45            │
│ (Turns red at < 1 minute)        │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ User clicks: [Verify Email]      │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ POST /api/auth/verify-otp        │
│ {                                │
│   email: "user@example.com",     │
│   otp: "123456"                  │
│ }                                │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Server Validation:               │
│ 1. OTP format valid (6 digits)?  │
│ 2. Email provided?               │
└──────────────────────────────────┘
  │ Valid
  ▼
┌──────────────────────────────────┐
│ Fetch EmailOTP from DB:          │
│ SELECT * FROM EmailOTP           │
│ WHERE email = "user@example.com" │
└──────────────────────────────────┘
  │
  ├─ NOT FOUND ─────────────────────┐
  │                                 │
  │                                 ▼
  │                       Return 404
  │                       "OTP not found"
  │
  ▼ FOUND
┌──────────────────────────────────┐
│ Check if expired:                │
│ if (expiresAt < now) {           │
│   return 410 (Gone)              │
│ }                                │
└──────────────────────────────────┘
  │ Not expired
  ▼
┌──────────────────────────────────┐
│ Check attempts:                  │
│ if (attempts >= 5) {             │
│   return 429 (Too many requests) │
│ }                                │
└──────────────────────────────────┘
  │ < 5 attempts
  ▼
┌──────────────────────────────────┐
│ Compare OTP:                     │
│ if (inputOTP === dbOTP) {        │
│   ✓ MATCH                        │
│ } else {                         │
│   ✗ NO MATCH                     │
│   attempts++                     │
│   return 401                     │
│ }                                │
└──────────────────────────────────┘
  │
  ├─ OTP MISMATCH ──────────────────┐
  │                                 │
  │                                 ▼
  │                       Return 401
  │                       "Invalid OTP"
  │                       "3 attempts left"
  │
  ▼ OTP MATCH
┌──────────────────────────────────┐
│ Update User:                     │
│ UPDATE User                      │
│ SET emailVerified = true         │
│ WHERE email = "user@example.com" │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Delete OTP:                      │
│ DELETE FROM EmailOTP             │
│ WHERE email = "user@example.com" │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Send Welcome Email (async):      │
│ To: user@example.com             │
│ Subject: "Welcome to HackHub!"   │
│ Body: Personalized welcome       │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Return 200 Success:              │
│ {                                │
│   success: true,                 │
│   message: "Email verified!",    │
│   user: { id, email, name }      │
│ }                                │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Frontend Actions:                │
│ 1. Show checkmark animation ✓    │
│ 2. Display success message       │
│ 3. Auto-redirect after 2 seconds │
└──────────────────────────────────┘
  │
  ▼
END (Account activated!)
```

---

## 🔄 Resend OTP Flow

```
START (User on /verify-otp)
  │
  ▼
┌──────────────────────────────────┐
│ User clicks: [Resend OTP]        │
│ (Button disabled for 5 minutes)  │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ POST /api/auth/resend-otp        │
│ {                                │
│   email: "user@example.com"      │
│ }                                │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Check if already verified:       │
│ SELECT user WHERE email = ...    │
│ if (emailVerified === true) {    │
│   return 400 (Already verified)  │
│ }                                │
└──────────────────────────────────┘
  │ Not verified yet
  ▼
┌──────────────────────────────────┐
│ Check rate limit:                │
│ SELECT * FROM EmailOTP           │
│ WHERE email = ... AND            │
│ createdAt > now - 5 minutes      │
│ if (exists) {                    │
│   return 429 (Rate limited)      │
│ }                                │
└──────────────────────────────────┘
  │ Can resend
  ▼
┌──────────────────────────────────┐
│ Delete old OTP:                  │
│ DELETE FROM EmailOTP             │
│ WHERE email = ...                │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Generate new OTP:                │
│ newOTP = generateOTP()           │
│ expires = now + 10 minutes       │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Save new OTP:                    │
│ INSERT INTO EmailOTP (            │
│   email, newOTP, newExpires      │
│ )                                │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Send OTP Email (async):          │
│ To: user@example.com             │
│ Subject: "Your New OTP Code"     │
│ Body: HTML with new OTP          │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Return 200 Success:              │
│ {                                │
│   success: true,                 │
│   message: "OTP sent to email"   │
│ }                                │
└──────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────┐
│ Frontend Actions:                │
│ 1. Show success toast/message    │
│ 2. Reset OTP input fields        │
│ 3. Start new 10-minute timer     │
│ 4. Disable resend button for 5m  │
└──────────────────────────────────┘
  │
  ▼
END (User sees new OTP in email)
```

---

## 🔒 Security Decision Tree

```
REQUEST → /api/auth/register
  │
  ├─ Is email format valid? ──NO─→ Return 400 Bad Request
  │
  ├─ Is password length >= 8? ──NO─→ Return 400 Bad Request
  │
  ├─ Is name length >= 2? ──NO─→ Return 400 Bad Request
  │
  ├─ Does email already exist? ──YES─→ Return 409 Conflict
  │
  └─ Valid inputs
       │
       ├─ Hash password (bcryptjs, 10 rounds)
       ├─ Create user (emailVerified = false)
       ├─ Generate 6-digit OTP
       ├─ Save OTP (10 min expiry)
       ├─ Send OTP email
       └─ Return 201 Created

REQUEST → /api/auth/verify-otp
  │
  ├─ Is OTP format 6 digits? ──NO─→ Return 400 Bad Request
  │
  ├─ Does OTP exist in DB? ──NO─→ Return 404 Not Found
  │
  ├─ Is OTP expired? ──YES─→ Return 410 Gone
  │
  ├─ Are attempts >= 5? ──YES─→ Return 429 Too Many Requests
  │
  ├─ Does OTP match? ──NO─→ Increment attempts, Return 401
  │
  └─ OTP correct
       │
       ├─ Set emailVerified = true
       ├─ Delete OTP record
       ├─ Send welcome email
       └─ Return 200 OK

REQUEST → /api/auth/resend-otp
  │
  ├─ Is email provided? ──NO─→ Return 400 Bad Request
  │
  ├─ Is user already verified? ──YES─→ Return 400 Bad Request
  │
  ├─ Within 5 min of last request? ──YES─→ Return 429 Rate Limited
  │
  └─ Can resend
       │
       ├─ Delete old OTP
       ├─ Generate new OTP
       ├─ Save OTP (10 min expiry)
       ├─ Send OTP email
       └─ Return 200 OK
```

---

## 📊 Data Flow Diagram

```
User Input
   │
   ▼
┌─────────────┐
│ Validation  │ ← Format, length, existence checks
└─────────────┘
   │
   ▼ Valid
┌─────────────┐
│ Processing  │ ← Hash password, generate OTP, etc.
└─────────────┘
   │
   ▼
┌─────────────┐
│ Database    │ ← Create user, save OTP
└─────────────┘
   │
   ├─→ Email Service ← Non-blocking send
   │   │
   │   ▼
   │   SMTP (Gmail)
   │   │
   │   ▼
   │   User's Email Inbox
   │
   ▼
┌─────────────┐
│ Response    │ ← JSON with status, message, data
└─────────────┘
   │
   ▼
Client
```

---

## 🎭 Component State Management

```
Registration Page (/auth/register)
├─ formData
│  ├─ name: string
│  ├─ email: string
│  ├─ password: string
│  └─ confirmPassword: string
├─ passwordStrength: 0-5
├─ errors: Record<string, string>
├─ isLoading: boolean
└─ isSubmitted: boolean

OTP Verification Page (/verify-otp)
├─ otpDigits: [string, string, string, string, string, string]
├─ timeLeft: number (seconds)
├─ attempts: number
├─ isVerifying: boolean
├─ isVerified: boolean
├─ error: string
├─ resendCooldown: number
└─ email: string (from URL)
```

---

## ⚡ Performance Optimizations

```
Database:
├─ Index on [email, expiresAt]
│  └─ Fast lookup of OTP expiry
├─ Cascade deletes on OTP
│  └─ Automatic cleanup
└─ Connection pooling
   └─ Efficient resource usage

Email:
├─ Async sending (non-blocking)
│  └─ Doesn't slow down response
├─ HTML templates
│  └─ Professional appearance
└─ Error handling
   └─ Silent failures don't break flow

API:
├─ Input validation early
│  └─ Fast rejection of invalid data
├─ Database indexes
│  └─ Quick lookups
└─ Efficient queries
   └─ Only fetch needed data
```

---

**This visual guide provides a complete understanding of the system architecture, data flows, and security decision paths.**

For implementation details, see: [OTP_SYSTEM_IMPLEMENTATION_INDEX.md](OTP_SYSTEM_IMPLEMENTATION_INDEX.md)
